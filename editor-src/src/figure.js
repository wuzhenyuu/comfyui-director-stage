/**
 * figure.js — 已删除（火柴人永久移除，2026-07-23）
 * 此文件仅保留空壳以维持 main.js 等的导入不报错。
 * 所有导出均为 no-op，火柴人功能不再可用。
 */
export const MAX_CHARACTERS = 0;
export class CharacterManager {
  constructor() { this.characters = new Map(); }
}
export function createJoints() { return []; }
export function createBones() { return []; }
export function updateBones() {}
export function applySpheresToBones() {}
